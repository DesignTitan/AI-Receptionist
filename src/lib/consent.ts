/**
 * Said in the first sentence of every outbound call, in every vertical.
 * Several US states require both parties to know a call is recorded; this is
 * a shared constant rather than per-vertical copy so no business can leave it
 * out. Also used by the simulator so demo transcripts show what a real call
 * says.
 */
export const RECORDING_NOTICE = "This call is recorded.";
