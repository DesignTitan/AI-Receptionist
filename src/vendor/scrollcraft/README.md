Vendored verbatim from ~/.codex/skills/scrollcraft/engine. Never edit: theme with
`--sc-*` tokens on the page root and write your own markup with `data-sc-*`.
The stylesheet is imported into a cascade layer from globals.css so its base
rules (body, selection, focus) never outrank the app's own.
